# Archive

These folders preserve earlier website iterations for reference.

- `web-old-v1` is the first static HTML/CSS/JS version.
- `web-static-final` is the final static HTML/CSS/JS version before the React rewrite.

The active website lives in `apps/web`.
